{
  "name": "social-media-nlp-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd FE-Social-Media && npm run build",
    "vercel-build": "cd BE-Social-Media && npx prisma generate && cd ../FE-Social-Media && pnpm run build && node -e \"require('fs').cpSync('dist', '../dist', {recursive: true})\""
  }
}
