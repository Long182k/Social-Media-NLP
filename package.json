{
  "name": "social-media-nlp-fullstack",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "FE-Social-Media",
    "BE-Social-Media"
  ],
  "scripts": {
    "build": "cd FE-Social-Media && npm run build",
    "vercel-build": "cd BE-Social-Media && npx prisma generate && cd ../FE-Social-Media && npm run build && node -e \"require('fs').cpSync('dist', '../dist', {recursive: true})\""
  },
  "devDependencies": {
    "prisma": "5.22.0"
  }
}
